# git-commit — Git 提交推送工作流 Skill

Git commit + push 全流程自动化。遵守 CLAUDE.md 红线规则，commit 自动做，push 必须问。

## 目录

```
git-commit/
├── SKILL.md          # Skill 定义
├── docs/             # 设计文档
└── scripts/          # （预留）
```

## 开发命令

```bash
# 查看 skill 内容
cat SKILL.md
```

## 提交前检查

- [ ] SKILL.md 中不包含敏感信息
- [ ] 触发词准确覆盖"提交/上传/push/commit"等场景
